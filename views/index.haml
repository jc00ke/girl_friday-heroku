
%form{ :method => :post }
  %label{ :for => 'message' } Message
  %input#message{ :type => :text, :name => :message }

  %input{ :type => :submit }

%a#clear{ :href => '#' }Clear messages

%p Below are the messages from the server
#messages
  - @messages.each do |msg|
    .message= msg
